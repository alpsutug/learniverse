# --- DataSource ---
spring.datasource.url=jdbc:postgresql://localhost:5432/workdb
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:1234}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Server ---
server.port=8010
spring.application.name=work

# --- i18n ---
spring.messages.basename=work_messages
spring.messages.encoding=UTF-8

# --- JWT ---
security.jwt.secret=${JWT_SECRET:s7nGvQ9xTbK2uM5wHrL8pJ4cAeYzR6q1}  # 32+ char
security.jwt.expiration=3600000

# --- Mail (forgot-password) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:alpsutug1999@gmail.com}
spring.mail.password=${MAIL_PASS:hufugbatwyunhgjr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
